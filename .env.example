# Path to OAuth Client Secret JSON file (downloaded from Google Cloud Console)
GSC_OAUTH_CLIENT_SECRET_PATH=/path/to/client_secret.json

# Optional: custom path for saved OAuth token (default: ~/.heyseo-gsc/token.json)
# GSC_TOKEN_PATH=/path/to/token.json
